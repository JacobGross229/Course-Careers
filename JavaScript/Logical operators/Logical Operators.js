/*
&& and: Tell us if both the left and the right are the same 
|| or: Tell us if both the left or the right is the same 
! Not: Reverse what the boolean is 
*/

// const and = x && y && z; 
// console.log(and);

const or = true || true;

console.log(or);

// const not = !true;
const not = !(5 < 4);

const and = (6 + 2 < 9) && (-9 + 27 % 3 == 2);

console.log(and); 

