let i = 0;
While (i < 10); {
    console.log(i);
    i++;
    console.log(i);
}

// ------------------------------------------------------------------------------------------------

const number = Number(prompt("Enter a number between 1 and 10: "));

while (!(number >= 1 && number<= 10)) {
    number = Number(prompt("Enter a number between one and ten: "));
}

console.log(number);

// ------------------------------------------------------------------------------------------------

let number1; 

do {
    number1 = Number(prompt("Enter a numver between one and ten: "));
} while (!(number1 >= 1 && number1 <= 10));

console.log(number1);

// ------------------------------------------------------------------------------------------------

let number2 = -1;

while (true) {
    number2 = Number(prompt("Enter a number between one and 10: "))
    if (number2 >= 1 && number <= 10) break;
}

console.log(number2);